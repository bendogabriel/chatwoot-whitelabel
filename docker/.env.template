# Nexa AI Platform - Environment Configuration Template
# Copy this file to .env and fill in your values

# ==========================================
# CLIENT INFORMATION
# ==========================================
CLIENT_NAME=nexa_inbox
BRAND_NAME=Nexa Inbox
BRAND_LOGO_URL=https://cdn.nexateam.com.br/logo.svg
BRAND_PRIMARY_COLOR=#1f93ff
SUPPORT_EMAIL=support@nexateam.com.br

# ==========================================
# DOMAINS
# ==========================================
CHATWOOT_DOMAIN=inbox.nexateam.com.br
CHATWOOT_URL=https://inbox.nexateam.com.br
ATLAS_DOMAIN=atlas.nexateam.com.br
DASHBOARD_DOMAIN=dashboard.nexateam.com.br
N8N_DOMAIN=n8n.nexateam.com.br
N8N_URL=https://n8n.nexateam.com.br

# ==========================================
# DOCKER IMAGES
# ==========================================
CHATWOOT_IMAGE=nexateam/chatwoot-custom:v3.15.0
ATLAS_IMAGE=nexateam/atlas-nexa:latest
DASHBOARD_IMAGE=nexateam/nexa-dashboard:latest

# ==========================================
# PORTS (For local development without Traefik)
# ==========================================
CHATWOOT_PORT=3000
ATLAS_PORT=4000
DASHBOARD_PORT=5000
N8N_PORT=5678

# ==========================================
# DATABASE (PostgreSQL)
# ==========================================
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_32_CHARS

# ==========================================
# REDIS
# ==========================================
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD

# ==========================================
# CHATWOOT CONFIGURATION
# ==========================================
# Generate with: openssl rand -hex 64
CHATWOOT_SECRET_KEY=CHANGE_ME_SECRET_KEY_MIN_64_CHARS

# Chatwoot API access (for Atlas integration)
# Generate in Chatwoot UI after first login
CHATWOOT_API_TOKEN=
CHATWOOT_ACCOUNT_ID=1
CHATWOOT_INBOX_ID=1

# ==========================================
# EMAIL (SMTP)
# ==========================================
SMTP_DOMAIN=gmail.com
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password

# ==========================================
# ATLAS NEXA CONFIGURATION
# ==========================================
# Supabase (or direct Postgres)
ATLAS_SUPABASE_KEY=CHANGE_ME_SUPABASE_ANON_KEY

# AI Services
OPENAI_API_KEY=sk-...
GOOGLE_AI_API_KEY=
ELEVENLABS_API_KEY=

# WhatsApp API
WHATSAPP_API_URL=https://evolution.nexateam.com.br
WHATSAPP_API_KEY=
WHATSAPP_PHONE_ID=

# Qualification Settings
QUALIFICATION_THRESHOLD=7
AUTO_HANDOFF_ENABLED=true

# ==========================================
# DASHBOARD CONFIGURATION
# ==========================================
# Generate with: openssl rand -hex 32
DASHBOARD_JWT_SECRET=CHANGE_ME_JWT_SECRET
DASHBOARD_SESSION_SECRET=CHANGE_ME_SESSION_SECRET

# ==========================================
# N8N CONFIGURATION
# ==========================================
# Generate with: openssl rand -hex 32
N8N_ENCRYPTION_KEY=CHANGE_ME_N8N_ENCRYPTION_KEY

# ==========================================
# TRAEFIK / SSL
# ==========================================
ACME_EMAIL=admin@nexateam.com.br

# ==========================================
# NOTES
# ==========================================
# 1. Replace all CHANGE_ME_* values with secure random strings
# 2. For Gmail SMTP, create an "App Password" at: https://myaccount.google.com/apppasswords
# 3. After first deployment, generate Chatwoot API token:
#    - Login to Chatwoot → Settings → Integrations → API → Platform → Create Token
# 4. Atlas Nexa will auto-create API token on first connection (if AUTO_HANDOFF_ENABLED=true)
